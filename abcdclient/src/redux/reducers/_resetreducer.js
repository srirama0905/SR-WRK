
 import {RESET_REDUX_SUCCESS}  from "../../utilities/_actiontypes";
 import initialState from "./_initialstate";

 export default function Resetreducer(state = initialState.ResetReducer, action) {
 /*
   switch (action.type) {
     case RESET_REDUX_SUCCESS:
       return  action.payload;    
        default:
       return state;
   }*/
 }
